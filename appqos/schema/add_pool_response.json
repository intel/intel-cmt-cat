{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "title": "REST API add pool",
  "description": "POST command response, URI /pools",
  "type": "object",

  "properties": {
    "id": {
      "description": "POOL ID",
      "$ref": "definitions.json#/uint"
    }
  },

  "required": ["id"]
}
